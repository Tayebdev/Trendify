class SplashConfig {
  static const logoAnimDuration = Duration(seconds: 2);
  static const appNameDelay = Duration(milliseconds: 800);
  static const taglineDelay = Duration(milliseconds: 1200);
  static const splashDuration = 3000; // total ms
}